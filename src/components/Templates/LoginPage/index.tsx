import { Container, Title } from "./styledComponents";

const LoginPage = () => {
  return (
    <Container component="section">
      <Title variant="h1">Login Page</Title>
    </Container>
  );
};

export default LoginPage;
