import { Container, Title } from "./styledComponents";

const RegisterPage = () => {
  return (
    <Container component="section">
      <Title variant="h1">Register Page</Title>
    </Container>
  );
};

export default RegisterPage;
